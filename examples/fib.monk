let fib = fn(x) {
  if (x == 0) {
    return 0;
  } else {
    if (x == 1) {
      return 1;
    } else {
      fib(x - 1) + fib(x - 2);
    }
  }
};

puts(fib(8));

let string_test = "hello from bytecode";
puts(string_test);
